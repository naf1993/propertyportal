{
  "name": "my-property-portal",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "start": "cd backend && npm run start",
    "start:prod": "cd backend && npm run start:prod",
    "start:dev": "cd backend && npm run start:dev",
    "build": "cd backend && npm run build",
    "test": "cd backend && npm run test",
 "heroku-postbuild": "cd backend && npm install && npm run build && npm install --only=prod"
  },
  "dependencies": {
    "@nestjs/core": "^10.0.0",
    "@nestjs/common": "^10.0.0",
    "@nestjs/platform-express": "^10.0.0",
    "@nestjs/config": "^3.3.0",
    "@nestjs/mongoose": "^10.1.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "cloudinary": "^2.5.1",
    "dotenv": "^16.4.7",
    "mongoose": "^8.9.2",
    "multer": "^1.4.5-lts.1",
    "reflect-metadata": "^0.2.0",
    "rxjs": "^7.8.1",
    "sharp": "^0.33.5",
    "streamifier": "^0.1.1",
    "uuid": "^11.0.4"
  },
  "engines": {
    "node": "14.x",
    "npm": "6.14.18"
  }
}
