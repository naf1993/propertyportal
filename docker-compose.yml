version: '3'
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"  # Expose port 5000 instead of 3000
    environment:
      - NODE_ENV=production
    # Additional environment variables, volumes, or commands as needed
